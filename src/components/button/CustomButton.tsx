import { Button } from "@mui/material";
import React from "react";

const CustomButton = () => {
    return (
        <div>
            <Button>Add</Button>
        </div>
    )
}

export default CustomButton;